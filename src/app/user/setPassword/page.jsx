import SetPassword from '@/components/user/SetPassword';
import React from 'react';

const page = () => {
    return (
        <div>
            <SetPassword/>
            
        </div>
    );
};

export default page;